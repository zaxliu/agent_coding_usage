# Archived Plan

This historical config-menu plan has been superseded.

Current behavior:

- remote configuration persists SSH host, user, port, labels, paths, and optional jump host fields
- remote password input is session-only and requested after SSH key authentication fails
- legacy remote password flags are cleaned up when configuration is saved
