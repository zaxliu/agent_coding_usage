# Archived Plan

This historical plan has been superseded by the current remote setup flow:

- temporary remote setup collects SSH host, user, port, and optional jump host fields
- SSH authentication now defaults to key-based login
- password retry is requested only after key authentication fails
