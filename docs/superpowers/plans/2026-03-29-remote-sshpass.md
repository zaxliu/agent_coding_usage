# Archived Plan

This plan is retained only as a historical marker.

The implementation has since moved to:

- SSH key authentication first
- password retry through Paramiko when key authentication fails
- session-only password caching
- no persistent remote password mode flags
